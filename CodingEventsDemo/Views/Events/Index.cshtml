<h1>Coding Events</h1>

<p>
    <a asp-controller="Events" asp-action="Add">Add Event</a>
</p>

@if (ViewBag.events.Count == 0)
{
    <p>No events yet!</p>
}

<table>
    @foreach (KeyValuePair<string, string> item in ViewBag.events)
    {
        <tr>
            <th>@item.Key</th>
            <th>@item.Value</th>
        </tr>
    }
</table>
